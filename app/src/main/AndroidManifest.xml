<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anil.projectlit">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserNavigator" />
        <activity
            android:name=".User_Navigate"
            android:label="@string/title_activity_user__navigate"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ControlPane"
            android:label="@string/title_activity_control_pane"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Scheduler" />
        <activity android:name=".UnitHis" />

        <service
            android:name=".SensorService"
            android:enabled="true" />

    </application>

</manifest>